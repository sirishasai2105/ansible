- name: install nginx and run
  hosts: web
  tasks:
  - name: installing nginx
    ansible.builtin.package:
      name: nginx
      state: installed
  - name: starting nginx
    ansible.builtin.service:
      name: nginx
      state: started