- name: install nginx and run
  hosts: web
  tasks:
    - name: install nginx
      ansible.builtin.package:
        name: nginx
        state: installed
        become: yes
    - name: run nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes